redis:
  # In v25+, the "architecture" parameter is critical. 
  architecture: standalone
  namespaceOverride: "redis"

  auth:
    enabled: false 

  master:
    persistence:
      enabled: false 
    # Optional: Resource limits to keep your Multipass VM healthy
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
