apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: hotel-app
spec:
  type: ClusterIP
  selector:
    app: hotel-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
